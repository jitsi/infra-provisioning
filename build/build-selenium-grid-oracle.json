{
  "variables": {
    "build_id": "",
    "environment": "",
    "ansible_ssh_user": "",
    "ansible_build_path": "../../infra-configuration/ansible",
    "base_image_ocid": "",
    "region": "",
    "oracle_region": "",
    "availability_domain": "",
    "subnet_ocid": "",
    "compartment_ocid": "",
    "tenancy_ocid": "",
    "shape": "",
    "ocpus": "",
    "memory_in_gbs": "",
    "connection_ssh_private_key_file": "",
    "ansible_python_interpreter": "",
    "connection_use_private_ip": "false",
    "connection_ssh_bastion_host": "",
    "tag_namespace": "",
    "ansible_deploy_tags": "",
    "ansible_skip_tags": "",
    "selenium_grid_image_name": "BuildSeleniumGrid-{{user `region`}}-{{user `environment`}}-{{timestamp}}"
  },
  "builders": [
    {
      "type": "oracle-oci",
      "availability_domain": "{{user `availability_domain`}}",
      "region": "{{user `region`}}",
      "base_image_ocid": "{{user `base_image_ocid`}}",
      "image_compartment_ocid": "{{user `tenancy_ocid`}}",
      "compartment_ocid": "{{user `compartment_ocid`}}",
      "subnet_ocid": "{{user `subnet_ocid`}}",
      "shape": "{{user `shape`}}",
      "shape_config": {
        "ocpus": "{{user `ocpus`}}",
        "memory_in_gbs": "{{user `memory_in_gbs`}}"
      },
      "image_name": "{{user `selenium_grid_image_name`}}",
      "communicator": "ssh",
      "ssh_username": "{{user `ansible_ssh_user`}}",
      "ssh_private_key_file": "{{user `connection_ssh_private_key_file`}}",
      "use_private_ip": "{{user `connection_use_private_ip`}}",
      "ssh_bastion_username": "{{user `ansible_ssh_user`}}",
      "ssh_bastion_host": "{{user `connection_ssh_bastion_host`}}",
      "ssh_bastion_private_key_file": "{{user `connection_ssh_private_key_file`}}",
      "defined_tags": {
        "{{user `tag_namespace`}}": {
          "Name": "{{user `selenium_grid_image_name`}}",
          "build_id": "{{user `build_id`}}",
          "TS": "{{timestamp}}",
          "Type": "SeleniumGrid",
          "environment_type": "dev"
        }
      }
    }
  ],
  "provisioners": [
    {
      "type": "ansible",
      "playbook_file": "{{user `ansible_build_path`}}/build-selenium-grid-oracle.yml",
      "user": "{{user `ansible_ssh_user`}}",
      "extra_arguments": [
        "-e",
        "ansible_ssh_user={{user `ansible_ssh_user`}}",
        "-e",
        "ansible_python_interpreter={{user `ansible_python_interpreter`}}",
        "-e",
        "cloud_provider=oracle",
        "-e",
        "hcv_environment={{user `environment`}}",
        "-e",
        "oracle_region={{user `oracle_region`}}",
        "--vault-password-file",
        ".vault-password.txt",
        "--tags",
        "{{user `ansible_deploy_tags`}}",
        "--skip-tags",
        "{{user `ansible_skip_tags`}}"
      ]
    }
  ]
}
